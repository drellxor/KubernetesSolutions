alertmanager:
  enabled: false

nodeExporter:
  enabled: false

prometheus:
  prometheusSpec:
    retention: 6h
    resources:
      requests: {cpu: 100m, memory: 400Mi}
      limits: {memory: 800Mi}

grafana:
  resources:
    requests: {cpu: 20m, memory: 128Mi}
