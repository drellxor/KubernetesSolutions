apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-pong-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ping-pong
  template:
    metadata:
      labels:
        app: ping-pong
    spec:
      volumes:
        - name: shared-volume
          persistentVolumeClaim:
            claimName: generic-claim
      containers:
        - name: ping-pong
          image: ping-pong:3.0
          volumeMounts:
            - mountPath: /usr/src/app/files
              name: shared-volume
