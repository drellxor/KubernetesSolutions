apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: shared-volume
          persistentVolumeClaim:
            claimName: project-claim
      containers:
        - name: todo-app
          image: PROJECT/APP
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: project-config
          volumeMounts:
            - mountPath: /usr/src/app/files
              name: shared-volume

